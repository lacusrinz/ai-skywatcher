version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    environment:
      - MOCK_MODE=true
      - USE_ONLINE_DATABASES=false
    restart: unless-stopped
