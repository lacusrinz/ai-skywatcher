# AI Skywatcher - 天文观测助手

AI-powered deep sky target recommender for astrophotography.

## 项目简介

AI Skywatcher 是一个专为天文摄影师设计的智能深空拍摄目标推荐工具。它根据您的位置、设备参数和可视区域,为您推荐最佳的深空天体拍摄目标。

## 功能特性

- 🌍 **智能定位**: 支持自动定位和手动设置观测地点
- 🔭 **设备配置**: 预设常见设备组合,自定义传感器和焦距参数
- 🎯 **可视区域**: 绘制并管理您的观测可视区域
- 🗺️ **交互式天空图**: 实时显示深空目标位置的 Canvas 天空图
- ⭐ **智能推荐**: 基于多维度评分的拍摄目标推荐
- ⏰ **时段筛选**: 今晚黄金、后半夜、黎明前时段推荐
- 📱 **响应式设计**: 支持桌面、平板、移动设备

## 技术栈

- **前端框架**: Vanilla JavaScript + HTML5
- **构建工具**: Vite
- **样式方案**: CSS Variables + CSS Modules
- **图表库**: Canvas API
- **天文计算**: astronomy-engine
- **状态管理**: 自定义 Store (Pub/Sub 模式)
- **离线存储**: IndexedDB + Service Worker

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

### 预览生产版本

```bash
npm run preview
```

## 项目结构

```
ai-skywatcher/
├── src/
│   ├── index.html           # 入口 HTML
│   ├── styles/
│   │   ├── variables.css    # CSS 变量(设计 tokens)
│   │   ├── reset.css        # 样式重置
│   │   ├── components.css   # 组件样式
│   │   └── layouts.css      # 布局样式
│   └── scripts/
│       ├── main.js          # 入口文件
│       ├── store.js         # 状态管理
│       ├── api.js           # API 接口
│       ├── utils/
│       │   └── canvas.js    # Canvas 天空图
│       └── data/
│           └── mock-data.js # Mock 数据
├── package.json
├── vite.config.js
└── README.md
```

## 核心组件

### Header (顶部导航栏)
- 显示当前观测地点和时间
- 日期选择器
- 设备配置下拉菜单

### ConfigPanel (左侧配置面板)
- 位置设置(自动/手动)
- 设备配置(预设/自定义)
- 可视区域管理

### SkyMap (中间天空图)
- Canvas 绘制的 360° 天空图
- 实时显示深空目标位置
- 时间轴预览和交互

### RecommendPanel (右侧推荐面板)
- 时段筛选(今晚黄金/后半夜/黎明前)
- 目标列表展示
- 推荐指数和详细信息

## API 接口

所有 API 接口使用 `/api/v1` 前缀:

- `POST /api/v1/locations/geolocate` - 自动定位
- `GET /api/v1/locations` - 获取保存的地点
- `POST /api/v1/locations` - 保存地点
- `GET /api/v1/equipment/presets` - 获取设备预设
- `POST /api/v1/equipment/calculate-fov` - 计算 FOV
- `GET /api/v1/targets` - 获取深空目标列表
- `POST /api/v1/recommendations` - 获取推荐目标
- `POST /api/v1/sky-map/data` - 获取天空图数据

## 开发文档

- [前端开发文档](./docs/plans/2025-01-22-frontend-development-guide.md)
- [后端设计文档](./docs/plans/2025-01-21-backend-design.md)
- [项目设计文档](./docs/plans/2025-01-21-deep-sky-target-recommender-design.md)

## 设计系统

### 颜色系统

- 主背景色: `#121212` (深空主题)
- 次要背景色: `#1A1A2E`
- 主强调色: `#10B981` (绿色,成功/推荐)
- 次强调色: `#3B82F6` (蓝色,链接/选中)

### 字体系统

- 基础字体: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto
- 等宽字体: 'SF Mono', Monaco, 'Cascadia Code', monospace

### 间距系统

- 使用 4px 基础单位的倍数: 4px, 8px, 12px, 16px, 20px, 24px...

## 贡献指南

1. Fork 本项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: add some amazing feature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## License

本项目采用 MIT 许可证。

## 致谢

- [astronomy-engine](https://github.com/cosinekitty/astronomy) - 天文计算库
- [Vite](https://vitejs.dev/) - 快速的前端构建工具
- 设计稿基于 MasterGo

---

**版本**: 1.0.0
**最后更新**: 2025-01-22
